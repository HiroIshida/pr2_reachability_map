from setuptools import setup

setup(
    name="pr2_reachability_map",
    version="0.0.0",
    description="experimental",
    author="Hirokazu Ishida",
    install_requires=["numpy", "torch"],
    packages=["pr2_reachability_map"],
)
